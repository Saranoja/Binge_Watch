""" Main module of the application.

Classes:

    InteractiveShell

Functions:

    read_valid_show_name(object) -> str
    read_valid_score(object) -> int
    read_valid_date(object) -> str
    print_initial(object)
    show_new_episodes(object)
    show_youtube_uploads(object)
    update_last_episode(object)
    add_show(object)
    set_snoozed_flag(object)
    update_score(object)
    solve_command(object, str)
    session_scope()
    convert_to_bool(str) -> bool
"""